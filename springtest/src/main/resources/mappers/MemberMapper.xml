<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.green.test.dao.MemberDAO">
 	<select id="getMember" resultType="kr.green.test.vo.MemberVO">
 	select * from member where id = #{id}
 	</select>
 	
 	<insert id="signup">
 		insert into member(id, pw, name, email, gender)
 		value(#{user.id},#{user.pw},#{user.name},#{user.email},#{user.gender}) 
 		<!-- 클래스에 속한 멤버변수는 클래스명.멤버변수로 써줘야함 -->
 	</insert>
 	<update id="updateMember">
 		update member set
 			pw = #{user.pw},
 			email = #{user.email},
 			gender = #{user.gender},
 			authority = #{user.authority}
 		where id = #{user.id}
 	</update>
</mapper>
